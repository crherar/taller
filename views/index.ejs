<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="author" content="Kodinger">
	<title>ITSEC - Yara Analyzer</title>
	<link rel="stylesheet" type="text/css" href="assets/css/bootstrap.min.css">
	<link rel="stylesheet" type="text/css" href="assets/css/login.css">

</head>

<body class="my-login-page">
	<section class="h-100">
		<div class="container h-100">
			<div class="row justify-content-md-center h-100" style="align-items: center;">
				<div class="card-wrapper">
					<div class="card fat">
						<div class="card-body">
							<h4 class="card-title">Login</h4>

							<form id="myForm" method="post" action="login">
							 
								<div class="form-group">
									<label for="email">Email</label>

									<input id="email" type="email" class="form-control" name="email" value="" required autofocus>
								</div>

								<div class="form-group">
									<label for="password">Password
									</label>
									<input id="password" type="password" class="form-control" name="password"> 
								</div>
								<div class="form-group no-margin">
									<input type="submit" class="btn btn-primary btn-block" value="Ingresar">
										<!-- Ingresar -->
							
								</div>
							</form>
						</div>
					</div>
					<div class="footer">
						Copyright &copy; ITSEC S.A. 2019
					</div>
				</div>
			</div>
		</div>
	</section>

	<script src="assets/js/jquery.min.js"></script>
	<script src="assets/js/bootstrap.min.js"></script>
	<!-- <script src="assets/js/my-login.js"></script> -->
	<script>
		$(document).ready(function () {
		  $('#myForm').on('submit', function(){
			  var form = $(this);
			  var url = form.attr('action');
			  var method = form.attr('method');
			  var data = form.serialize();

		    // fetch(url, {
			// method: 'POST', // or 'PUT'
			// body: JSON.stringify(data), // data can be `string` or {object}!
			// headers:{
			// 	'Content-Type': 'application/json'
			// }
			// }).then((res) => {
				
            // // if (res.status == 200) {
			// 	window.localStorage.setItem("token", res.token);
			// // 	console.log(res);
            // //    localStorage.setItem("token", res.data.token);
			// //    document.window.location.href = '/clientes';
			// //    headers.append('access_token',res.data.token);
            // //}
			// })
			// .catch(error => console.error('Error:', error))
			// .then(response => console.log('Success:', response));

			  $.ajax({
				  url: url,
				  method: method,
				  data: data,
				  success: function(res) {
					console.log(res);
					window.localStorage.setItem("token", res.token);
					//window.location.replace(res.url);
				  }
			  });
			});
		});
		
		

							
				</script>
</body>


</html>